#include<stdio.h>

int main()
{
    int a[50],size,i;
    pritnf("\nenter the array size : ");
    scanf("%d",&size);

    pritnf("\nEnter array element : \n ");
    for(i= 0; i < size ; i++)
    {
        scanf("%d",&a[i]);
    }

    for(i= 0; i < size ; i++)
    {
        scanf("%d",&a[i]);
    }

    return 0;
}
