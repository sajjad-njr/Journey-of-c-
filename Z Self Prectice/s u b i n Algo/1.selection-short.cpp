#include<iostream>
using namespace std ;

void Sc(int a[], int n)
{
    int i, j, indexMin, temp;

    for(i = 0 ; i < n - 1 ; i++)
    {
        indexMin = i;
        for(j = 0 ; j < n ; j++)
        {

        }
    }
}
