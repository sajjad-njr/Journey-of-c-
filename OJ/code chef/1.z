#include<stdio.h>
int main()
{
    int i,n,num,sum=0;
    scanf("%d",&n);
    for(i=1; i<=n; i++)
    {
        scanf("%d",&num);
        sum=0;
        while(num>0)
        {
            sum+=(num%10);
            num=num/10;
        }
        printf("%d\n",sum);
    }
    return 0;
}
